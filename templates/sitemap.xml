<!-- Blog Yazıları -->
{% for post in blog_posts %}
<url>
    <loc>{{ base_url }}/blog/{{ post.slug }}</loc>
    <xhtml:link rel="alternate" hreflang="tr" href="{{ base_url }}/blog/{{ post.slug }}?lang=tr"/>
    <xhtml:link rel="alternate" hreflang="en" href="{{ base_url }}/blog/{{ post.slug }}?lang=en"/>
    <lastmod>
        {% if post.updated_at or post.created_at %}
            {{ (post.updated_at or post.created_at).split(' ')[0] if post.updated_at is string else (post.updated_at or post.created_at).strftime('%Y-%m-%d') }}
        {% else %}
            {{ lastmod }}
        {% endif %}
    </lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
</url>
{% endfor %}